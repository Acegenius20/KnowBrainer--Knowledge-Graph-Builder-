version: "3.8"

services:
  mongodb:
    image: mongo
    container_name: knowbrainer_db
    command: ["mongod", "--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
